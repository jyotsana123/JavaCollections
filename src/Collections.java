
public class Collections {

	public static void main(String[] args) {
		// TODO Auto-generated method stub

		/*The Java Collections Framework is a collection of interfaces and classes which
		helps in storing and processing the data efficiently.
		This framwork has several useful classes which have tons of useful functions which makes a 
		programmer task super easy. */
		
		/* 1. List Collection
		 * A list is an ordered collection(Sometimes called a sequence). Lists may contain duplicate elements.
		 * Below are the classes implement List interface.
		 * -ArrayList
		 * -LinkedList
		 * -Vector
		 * 
		 * 2. Set Collection
		 * A set is a collection that can not contain duplicate elements.
		 * however it makes no guarantees concerning the order of iteration.
		 * Below are classes implement Set interface.
		 * -HashSet
		 * -LinkedHashSet
		 * -TreeSet
		 * 
		 * 3. Map
		 * A map is an object that mapes keys to value. A map can not contain duplicate keys.
		 * Below are main implementation of map interface
		 * -HashMap
		 * -TreeMap
		 * -LinkedHashMap
		 */
		
		
	}

}
